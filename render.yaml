services:
  - type: web
    name: telegram-bot
    env: python
runtime: python-3.11
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT
    autoDeploy: true
